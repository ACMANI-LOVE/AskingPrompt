/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/AskingPrompt',
  assetPrefix: '/AskingPrompt/',
  distDir: "out",
  output: "export",
  trailingSlash: true,
};

export default nextConfig;
